# Global memory index

One line per memory file in `global/`. Read this index first; open
detail files only when relevant. Make line-level edits; never rewrite a
file wholesale. Re-read a file immediately before writing to it.

Project-specific memory does NOT live here — it lives in each project's
`projects/<slug>/notes/` directory.

<!-- Add entries as: - [Title](global/file.md) — one-line hook -->
